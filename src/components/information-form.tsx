"use client";
import { FormEvent, useState } from "react";
const options = [["sighting", "Creo que la vi."], ["possible_trapped", "Puede estar atrapada."], ["possible_deceased", "Tengo información sobre un posible fallecimiento."], ["correction", "Quiero corregir un dato."], ["other_information", "Tengo otra información."]];
export function InformationForm({ caseId }: { caseId: string }) { const [done, setDone] = useState(""), [error, setError] = useState(""), [busy, setBusy] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setError(""); const f = new FormData(e.currentTarget); const body = { caseId, reportType: f.get("reportType"), eventAt: f.get("eventAt"), location: f.get("location"), description: f.get("description"), consent: f.get("consent") === "on", website: f.get("website") }; const r = await fetch("/api/reports", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const d = await r.json(); setBusy(false); if (!r.ok) return setError(d.message); setDone(d.trackingCode); e.currentTarget.reset(); } if (done) return <div className="success" role="status"><h3>Información recibida</h3><p>Tu código de seguimiento es <strong>{done}</strong>. Un equipo autorizado la revisará.</p></div>; return <form onSubmit={submit} className="report-form"><input className="honeypot" aria-hidden="true" tabIndex={-1} name="website" autoComplete="off" /><label>Tipo de información<select name="reportType" defaultValue="sighting">{options.map(([value, label]) => <option key={value} value={value}>{label}</option>)}</select></label><label>Fecha y hora aproximada<input name="eventAt" type="datetime-local" /></label><label>Lugar aproximado<input name="location" maxLength={240} /></label><label>Descripción<textarea name="description" required minLength={10} maxLength={3000} /></label><label className="check"><input name="consent" type="checkbox" required /> Autorizo el tratamiento de esta información para su revisión.</label>{error && <p role="alert" className="form-error">{error}</p>}<button className="button" disabled={busy}>{busy ? "Enviando…" : "Enviar información"}</button></form>; }
