"use client";
import { FormEvent, useState } from "react";
export default function ReportPage() { const [step, setStep] = useState(1), [done, setDone] = useState(""), [error, setError] = useState(""), [busy, setBusy] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); const f = new FormData(e.currentTarget); if (step < 3) return setStep(step + 1); setBusy(true); setError(""); const body: Record<string, unknown> = Object.fromEntries(f); body.isMinor = f.get("isMinor") === "on"; body.consent = f.get("consent") === "on"; body.goodFaith = f.get("goodFaith") === "on"; const r = await fetch("/api/reports", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const d = await r.json(); setBusy(false); if (!r.ok) return setError(d.message); setDone(d.trackingCode); } if (done) return <section className="form-page"><div className="success" role="status"><h1>Reporte recibido</h1><p>Tu código de seguimiento es <strong>{done}</strong>.</p><p>El caso no se publica automáticamente. Un equipo revisará la información y podrá contactarte.</p></div></section>; return <section className="form-page"><h1>Reportar a una persona desaparecida</h1><p className="lead">Completa lo que sepas. No compartiremos tus datos de contacto.</p><p className="steps" aria-label={`Paso ${step} de 3`}>Paso {step} de 3</p><form className="report-form" onSubmit={submit}><input className="honeypot" aria-hidden="true" tabIndex={-1} name="website" autoComplete="off" />{step === 1 && <fieldset><legend>¿A quién buscas?</legend><label>Nombre completo<input name="fullName" required minLength={3} maxLength={140} /></label><label>Alias o cómo le conocen<input name="alias" maxLength={80} /></label><label>Edad aproximada<input name="approximateAge" type="number" min="0" max="120" /></label><label className="check"><input name="isMinor" type="checkbox" /> Es menor de edad</label><p className="privacy-note">Para proteger a menores, no publiques dirección, colegio, teléfono ni datos de su tutor.</p></fieldset>}{step === 2 && <fieldset><legend>Última vez que fue vista</legend><label>Fecha<input name="lastSeenDate" type="date" required /></label><label>Hora aproximada<input name="lastSeenTime" type="time" /></label><label>Lugar aproximado<input name="location" required maxLength={240} /></label><label>Ropa que llevaba<textarea name="clothing" maxLength={800} /></label><label>Características distintivas<textarea name="features" maxLength={800} /></label><label>¿Qué ocurrió?<textarea name="circumstances" required minLength={10} maxLength={3000} /></label></fieldset>}{step === 3 && <fieldset><legend>Contacto y consentimiento</legend><label>Tu nombre (privado)<input name="reporterName" required /></label><label>Teléfono (privado)<input name="phone" inputMode="tel" /></label><label>Correo (privado)<input name="email" type="email" /></label><label>Medio preferido<select name="preferredContact" defaultValue="either"><option value="either">Cualquiera</option><option value="phone">Teléfono</option><option value="email">Correo</option></select></label><label className="check"><input name="consent" type="checkbox" required /> Autorizo procesar esta información para revisar el caso.</label><label className="check"><input name="goodFaith" type="checkbox" required /> Confirmo que la información se entrega de buena fe.</label></fieldset>}{error && <p role="alert" className="form-error">{error}</p>}<div className="form-actions">{step > 1 && <button type="button" className="button secondary" onClick={() => setStep(step - 1)}>Atrás</button>}<button className="button" disabled={busy}>{step === 3 ? (busy ? "Enviando…" : "Enviar reporte") : "Continuar"}</button></div></form></section>; }
