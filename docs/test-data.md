# Datos de prueba

Los 15 registros de `supabase/seed.sql` son totalmente ficticios. Antes de ejecutarlo en un proyecto demo, defina `app.enable_test_data=true` para la sesión SQL. Genera los avatares sintéticos con `npm run generate:avatars`. No ejecute ni habilite estos datos en producción.
